import { render } from "@/components";

const rawMeta = {
  index: {
    title: "Overview",
  },
  native: {
    title: "Native (Api Class)",
  },
  fastapi: {
    title: "FastAPI",
  },
};

export default render(rawMeta);
