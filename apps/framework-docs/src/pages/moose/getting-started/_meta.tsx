import { render } from "@/components";

const meta = {
  quickstart: "5-Minute Quickstart",
  "add-to-app": "Add to Existing Application",
  "from-clickhouse": "Use with Existing ClickHouse",
};

export default render(meta);
