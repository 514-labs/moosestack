---
title: Stream.send()
description: Reference for publishing records from code that can import a `Stream` directly with `Stream.send()`
order: 6
category: streaming
---

import { Callout, LanguageTabs, LanguageTabContent } from "@/components/mdx";

# Stream.send()

Use `Stream.send()` to publish one or more records to an existing `Stream` when the producer can import your Moose SDK models and `Stream` definition directly. If the producer should only send JSON over HTTP, use an [Ingest API](/moosestack/apis/ingest-api) instead.

This page covers in-process producers. To define the stream itself, start with [Stream](/moosestack/streaming/create-stream).

## When to use

<table>
  <thead>
    <tr>
      <th>Use</th>
      <th>Typical scenarios</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>Stream.send()</code></td>
      <td>An existing app in your monorepo or <a href="/moosestack/workflows">Moose workflows</a>, where you can import the stream definition and other Moose objects directly.</td>
    </tr>
    <tr>
      <td><a href="/moosestack/apis/ingest-api">Ingest API</a></td>
      <td>Services that push events into your system when you do not control that codebase or cannot use Moose objects or clients there directly.</td>
    </tr>
  </tbody>
</table>

## Basic usage

`Stream.send()` accepts either a single record or an array of records:

<LanguageTabs>
  <LanguageTabContent value="typescript">
```ts filename="app/publish.ts" copy
import { Stream } from "@514labs/moose-lib";

interface UserEvent {
  id: string;
  userId: string;
  timestamp: Date;
  eventType: string;
}

const events = new Stream<UserEvent>("events");

// Send a single record
await events.send({
  id: "evt_1",
  userId: "user_123",
  timestamp: new Date(),
  eventType: "click",
});

// Send array of records
await events.send([
  { id: "evt_2", userId: "user_456", timestamp: new Date(), eventType: "view" },
  { id: "evt_3", userId: "user_789", timestamp: new Date(), eventType: "signup" },
]);
```
  </LanguageTabContent>
  <LanguageTabContent value="python">
```py filename="app/publish.py" copy
from datetime import datetime

from moose_lib import Stream
from pydantic import BaseModel

class UserEvent(BaseModel):
    id: str
    user_id: str
    timestamp: datetime
    event_type: str

events = Stream[UserEvent]("events")

events.send(UserEvent(
    id="evt_1",
    user_id="user_123",
    timestamp=datetime.now(),
    event_type="click",
))

events.send([
    UserEvent(id="evt_2", user_id="user_456", timestamp=datetime.now(), event_type="view"),
    UserEvent(id="evt_3", user_id="user_789", timestamp=datetime.now(), event_type="signup"),
])
```
  </LanguageTabContent>
</LanguageTabs>

## Behavior

- Passing an array publishes multiple stream records, not one array payload.
- Passing `null`, `undefined`, or an empty array sends nothing.
- `Stream.send()` publishes to the stream topic. If the stream also has a destination table, records become queryable only after Moose syncs them into ClickHouse.

<Callout type="info" title="Schema Registry JSON works automatically">
If your stream uses `schemaConfig.kind = "JSON"`, Moose publishes records with the Confluent Schema Registry envelope automatically. See [Schema Registry](/moosestack/streaming/schema-registry).
</Callout>

## Requirements

- If the stream uses `schemaConfig.kind = "JSON"`, configure `schema_registry_url` before calling `send()`.
- Only JSON Schema is supported for Schema Registry streams today.
- In Python, values passed to `send()` must be instances of the stream model type.
- In Python, Schema Registry JSON support requires `confluent-kafka[json,schemaregistry]`.

## Related capabilities

- [Stream](/moosestack/streaming/create-stream) for defining the stream resource
- [Sync to table](/moosestack/streaming/sync-to-table) when records should land in ClickHouse
- [Schema registry](/moosestack/streaming/schema-registry) when payloads should use Confluent Schema Registry
- [Ingest API](/moosestack/apis/ingest-api) for external HTTP ingestion into streams
