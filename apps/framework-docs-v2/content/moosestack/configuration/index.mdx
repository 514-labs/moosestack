---
title: Configuration
description: Configure MooseStack with an engine-neutral contract
order: 10
---

# Configuration

MooseStack configuration has two layers:

1. `moose.config.toml` for shared project contract.
2. `MOOSE_*` environment variables for environment overrides and secrets.

## Core contract

```toml
[olap]
engine = "clickhouse" # clickhouse | duckdb | snowflake
profile = "remote"    # local | remote
connection = "https://user:password@host:8443/db"

[dev_infra]
profile = "remote_hybrid" # local | remote_hybrid | remote_full
```

## Environment variable mapping

| Config key          | Environment variable       |
| ------------------- | -------------------------- |
| `olap.engine`       | `MOOSE_OLAP__ENGINE`       |
| `olap.profile`      | `MOOSE_OLAP__PROFILE`      |
| `olap.connection`   | `MOOSE_OLAP__CONNECTION`   |
| `dev_infra.profile` | `MOOSE_DEV_INFRA__PROFILE` |

## Read next

- [Engine and Dev Profile](/moosestack/configuration/engine-and-dev-profile)
- [Dev Environment](/moosestack/configuration/dev-environment)
- [Redpanda Configuration](/moosestack/configuration/redpanda)
- [Redis Configuration](/moosestack/configuration/redis)
