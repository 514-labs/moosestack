---
title: Configure Moose for Cloud
description: Configure MooseStack for remote-first cloud deployments
order: 2
category: deploying
---

# Configure Moose for Cloud

Use engine-neutral configuration for cloud environments.

## Config file

```toml
[olap]
engine = "clickhouse"
profile = "remote"
connection = "https://user:password@host:8443/db"

[dev_infra]
profile = "remote_full"
```

## Environment variables

```bash
MOOSE_OLAP__ENGINE=clickhouse
MOOSE_OLAP__PROFILE=remote
MOOSE_OLAP__CONNECTION=https://user:password@host:8443/db
MOOSE_DEV_INFRA__PROFILE=remote_full
```

## Deploy workflow

```bash
moose generate migration --olap-engine clickhouse --olap-url "$MOOSE_OLAP__CONNECTION" --save
moose migrate --olap-engine clickhouse --olap-url "$MOOSE_OLAP__CONNECTION"
moose prod
```
