### Example Code Snippet

Here's a simple data model example in TypeScript:

```typescript
export interface ExampleEvent {
  id: Key<string>;
  name: string;
  timestamp: Date;
}
```

<Callout type="info">
This example shows a basic event interface with a primary key.
</Callout>
