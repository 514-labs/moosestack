---
title: Optimizing ClickHouse Performance with AI
description: Profile a slow query, compare optimized and baseline preview databases, and review the benchmark evidence plus migration plan before merge.
languages: ["typescript"]
---

import { Callout } from "@/components/mdx";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";

# Optimizing ClickHouse Performance with AI

Use this guide when a ClickHouse query is slow and you want the `514` agent harness to run an end to end optimization workflow. The agent does the heavy lifting: it profiles the query, creates two preview databases forked from production, applies the optimization to one of them, runs the benchmark, and prepares the PR artifacts for review. Because both preview databases start from the same production slice, you can run the same benchmark against both and make a fair comparison.

Your role is to make sure the CLI and agent harness are set up in your environment, then review the agent's work at the end. The harness gives you the two artifacts that matter for that review: a baseline preview fork and an optimized preview fork for rerunning the benchmark, and a reviewable `migrations/plan.yaml` so you can confirm the schema change is safe to merge and will not accidentally drop data.

<Callout type="info" title="What success looks like">
- A PR with schema changes and committed benchmark artifacts
- Baseline and optimized preview databases benchmarked with the same test
- A reviewed `migrations/plan.yaml` that matches what you intend to ship
</Callout>

This guide assumes:

- Your MooseStack app is already deployed on Fiveonefour
- The query path you want to optimize already exists in code, usually as `sql` or `defineQueryModel` / `buildQuery`

### Setup

**Setup is the control layer.** The workflow is going to run real `514` commands against a real Fiveonefour project, and the agent will inherit the same session and repo link. Get this wrong and the rest of the guide is noise.

You need: working `514 --version`, `514 auth whoami` for the right org, and `514 link` (or [`514 project link`](/hosting/cli/projects/link)) from the repo root to the correct `org/project`.

Order: install, sign in, link.

<GuideStepper id="optimize-ch-setup" persist>
<GuideStepper.Step
  id="install-514-cli"
  number={1}
  title="Install the 514 CLI"
  summary="Install the CLI from Hosting and verify it is on your PATH."
>
<GuideStepper.WhatYouNeed>
- A local clone of the MooseStack app repository
- [Hosting](/hosting) docs if install fails
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- `514` installed and `514 --version` working
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Install the 514 CLI">
From [Hosting](/hosting), install the `514` CLI:

```bash
bash -i <(curl -fsSL https://fiveonefour.com/install.sh) 514
```
</GuideStepper.Checkpoint>
<GuideStepper.Checkpoint title="Verify installation">

```bash
514 --version
```

</GuideStepper.Checkpoint>
</GuideStepper.Step>

<GuideStepper.Step
  id="sign-in-514"
  number={2}
  title="Sign in to the 514 CLI"
  summary="Establish a session so project and deployment commands work."
>
<GuideStepper.WhatYouNeed>
- Install step complete
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- A confirmed account via `514 auth whoami`
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Authenticate">
```bash
514 auth login
514 auth whoami
```

Browser opens for login, then `514 auth whoami` shows your account. No browser? Sign in at `https://www.fiveonefour.boreal.cloud/login` and retry. [`514 auth login`](/hosting/cli/auth/login), [`514 auth`](/hosting/cli/auth).
Related docs:

- [`514 auth login`](/hosting/cli/auth/login)
- [`514 auth`](/hosting/cli/auth)

</GuideStepper.Checkpoint>
</GuideStepper.Step>

<GuideStepper.Step
  id="link-fiveonefour-project"
  number={3}
  title="Link your local repo to your Fiveonefour project"
  summary="Align local workspace with your hosted org and project."
>
<GuideStepper.WhatYouNeed>
- Access to the Fiveonefour project that hosts the deployment you want to optimize
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- Local configuration that points at the right `org/project` for CLI and agents
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Link your local project to your Fiveonefour project">
Make sure you are in the root of your MooseStack project directory and run:

```bash
514 project link <org>/<project>
```

Related docs:

- [`514 project link`](/hosting/cli/projects/link)
- [Fiveonefour hosting to local setup](/hosting/workflow/fiveonefour-to-local-setup)
- [Setup failures](/hosting/troubleshooting/setup-failures)

</GuideStepper.Checkpoint>
</GuideStepper.Step>
</GuideStepper>

### Optimization workflow

**This section gets the 514 harness set up with your coding agent and runs the optimization workflow.** After `514 agent init`, the workflow uses the `514` CLI to inspect ClickHouse metrics and logs, profile the slow path, and propose fixes based on what it actually sees. Part of that run is creating a **Vitest benchmark test** for the optimized path. That test produces the report you review at the end, which is the part that matters. The same test is meant to run against two preview databases forked from production: one baseline fork that stays unchanged, and one preview fork where the optimization is applied.

Watch three things as it runs:

- **The harness is active:** `514 agent init` succeeds and the skills and MCP wiring match your setup.
- **The profiling target is clear:** name the slow query or endpoint and confirm the perf skill, not a generic refactor.
- **The validation path is visible:** raw SQL, preview deploys, and any re-seed caused by `ORDER BY` or table layout changes should all be explicit.

<GuideStepper id="optimize-ch-workflow" persist>
<GuideStepper.Step
  id="agent-init-harness"
  number={1}
  title="Run 514 agent init"
  summary="Pull in 514 skills and scaffold the agent harness for your repo."
>
<GuideStepper.WhatYouNeed>
- Setup complete: CLI installed, signed in, project linked
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- Agent skill packs (including ClickHouse and performance-related workflows where applicable)
- MCP and harness files your coding agent uses to run `514`-backed workflows
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Initialize the harness">

```bash
514 agent init
```

This pulls skills from the 514 registry and sets up the agent harness for your project.
</GuideStepper.Checkpoint>
</GuideStepper.Step>

<GuideStepper.Step
  id="ask-copilot-optimize"
  number={2}
  title="Ask your copilot to optimize a query"
  summary="Prompt your coding agent and confirm the performance optimization skill."
>
<GuideStepper.WhatYouNeed>
- A deployed MooseStack app on Fiveonefour (the slow query path in code)
- Your coding agent (Cursor, Claude Code, Copilot in your editor, and so on) opened on this repo
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- The agent selects the performance optimization skill (often named `514--perf-optimize` or `perf-optimize` depending on client)
- Explicit confirmation before it runs a long optimization workflow
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Prompt the agent">
Ask for a ClickHouse performance optimization on your deployed app, and point it at the slow query or endpoint. Example:

```text
Optimize ClickHouse performance for this MooseStack app on Fiveonefour.
Profile the slow query path, propose schema changes, validate on baseline and optimized preview databases,
and prepare a PR with benchmark evidence and migrations/plan.yaml review context.
```

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Confirm the performance optimization skill">
When the agent proposes the perf optimization skill, confirm. It should drive profiling, schema work, benchmarks, and the PR. If the bundle name differs, accept only if it is the same harness you got from `514 agent init`.

</GuideStepper.Checkpoint>
</GuideStepper.Step>

<GuideStepper.Step
  id="optimization-workflow-runs"
  number={3}
  title="Let the optimization workflow run"
  summary="The agent runs setup through ship: profile, optimize, verify on baseline and optimized preview databases, then open a PR."
>
<GuideStepper.WhatYouNeed>
- Patience for branch pushes and preview deployments
- Willingness to approve raw `514 clickhouse query` when the skill prompts you
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- A feature branch with data model and test changes
- Benchmark runs against baseline and optimized preview databases
- A pull request draft or ready for review
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Expect five internal stages">
The workflow follows `SETUP → PROFILE → OPTIMIZE → VERIFY → SHIP`.

- **SETUP:** CLI session, project, deployment, baseline DDL.
- **PROFILE:** Slow queries, benchmark candidates, baseline `EXPLAIN`, re-seed risk.
- **OPTIMIZE:** Moose models, branch pushes, baseline fork, optimized fork.
- **VERIFY:** Schema diff, `EXPLAIN`, storage, timed runs on both preview databases.
- **SHIP:** PR with evidence and summary.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Approve gated ClickHouse commands">
Guardrailed `514 agent ...` runs without extra approval. Raw `514 clickhouse query` (DDL, `EXPLAIN`, diagnostics, re-seed) should prompt you. Approve when the SQL matches intent.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Watch for optimized preview re-seeds">
If `ORDER BY` or similar forces a rebuild, the optimized preview may need a re-seed before benchmarks are trustworthy. The PR should say if that happened.

</GuideStepper.Checkpoint>

<Callout type="info" title="What good evidence looks like">
Example: index use, fewer granules read (for example selective condition vs full scan, high `granuleSkipPct`), lower server-side latency. Your PR should show that kind of structural move in `EXPLAIN`, not only a headline number.
</Callout>
</GuideStepper.Step>
</GuideStepper>

### PR review

**This is the part that matters.** The agent can open a PR. You still decide what ships. Your job here is to check that the change is safe and that the evidence matches reality.

- **Performance:** Do benchmarks and `EXPLAIN` back the claim, not just the PR text?
- **Destructive ops:** Does `ORDER BY` or ordering change imply a rebuild you accept? Read `migrations/plan.yaml` with that in mind.

**Read `migrations/plan.yaml` end to end.** If `ORDER BY` or a rebuild is involved, expect a **backfill** or migration path, not only a new table. Keep the **old table** until the new one is safe. Populate and verify before drop or rename.

Order: benchmarks, then plan and diff, then optional reruns.

<GuideStepper id="optimize-ch-pr" persist>
<GuideStepper.Step
  id="review-benchmark-results"
  number={1}
  title="Review the benchmark results"
  summary="Read the performance test and benchmark JSON to verify the evidence in the PR."
>
<GuideStepper.WhatYouNeed>
- The PR the agent opened
- The committed performance test and any `reports/benchmark-details-*.json` attached or generated in CI for both the baseline preview fork and the optimized preview fork
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- Confidence that the optimized branch actually beats the baseline for the same query path, and that the `EXPLAIN` output supports merging
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Identify the optimized report and the baseline report">
Pair two artifacts: **optimized** (preview fork with the schema change) and **baseline** (preview fork from production with no schema change). The report usually does not include a branch field. In practice, the **database name contains the branch slug**, so that is the field to inspect first.

<Tabs defaultValue="optimized-report">
  <TabsList>
    <TabsTrigger value="optimized-report">Optimized</TabsTrigger>
    <TabsTrigger value="baseline-report">Baseline</TabsTrigger>
  </TabsList>
  <TabsContent value="optimized-report">

```json
{
  "target": {
    "host": "cluster.clickhouse.boreal.cloud",
    "database": "514-demos-planes-perf-opti-1c360"
  }
}
```

  </TabsContent>
  <TabsContent value="baseline-report">

```json
{
  "target": {
    "host": "cluster.clickhouse.boreal.cloud",
    "database": "514-demos-planes-perf-base-59be4"
  }
}
```

  </TabsContent>
</Tabs>

Both reports must use the **same benchmark file** and query path, different targets only. No clear baseline means no real before/after.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Read the performance test">
Open the committed test, often `e2e-tests/tests/moose-performance.e2e.test.ts`, and make sure both reports came from that same file. If you want the full field map while you review, use the quick reference at the end of this page.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Compare index condition and granule skipping">
Start with `tests.explainCategoryFilter.explain`. When index use is the goal, `indexCondition` should not be only `"true"`, and the optimized report should skip more granules than the baseline.

<Tabs defaultValue="optimized-explain">
  <TabsList>
    <TabsTrigger value="optimized-explain">Optimized</TabsTrigger>
    <TabsTrigger value="baseline-explain">Baseline</TabsTrigger>
  </TabsList>
  <TabsContent value="optimized-explain">

```json
{
  "tests": {
    "explainCategoryFilter": {
      "explain": {
        "indexCondition": "and((current_database in ['db_123']), (timestamp >= 1710200000))",
        "selectedGranules": 120,
        "totalGranules": 480,
        "granuleSkipPct": 75
      }
    }
  }
}
```

  </TabsContent>
  <TabsContent value="baseline-explain">

```json
{
  "tests": {
    "explainCategoryFilter": {
      "explain": {
        "indexCondition": "true",
        "selectedGranules": 480,
        "totalGranules": 480,
        "granuleSkipPct": 0
      }
    }
  }
}
```

  </TabsContent>
</Tabs>

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Compare latency and resource usage">
Check `tests.baseline.p50`, `tests.baseline.p95`, and the per-run objects in `tests.baseline.profiles`. The `profiles` array is where the useful resource metrics live.

<Tabs defaultValue="optimized-latency">
  <TabsList>
    <TabsTrigger value="optimized-latency">Optimized</TabsTrigger>
    <TabsTrigger value="baseline-latency">Baseline</TabsTrigger>
  </TabsList>
  <TabsContent value="optimized-latency">

```json
{
  "tests": {
    "baseline": {
      "p50": 38,
      "p95": 44,
      "profiles": [
        {
          "durationMs": 42,
          "readRows": 180000,
          "readBytes": 12000000,
          "memoryUsage": 6400000,
          "diskReadUs": 1200
        }
      ]
    }
  }
}
```

  </TabsContent>
  <TabsContent value="baseline-latency">

```json
{
  "tests": {
    "baseline": {
      "p50": 119,
      "p95": 126,
      "profiles": [
        {
          "durationMs": 121,
          "readRows": 920000,
          "readBytes": 68000000,
          "memoryUsage": 15400000,
          "diskReadUs": 8800
        }
      ]
    }
  }
}
```

  </TabsContent>
</Tabs>

Good sign: lower `p95`, fewer rows read, and lower memory usage in the optimized report.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Check filter regression ratios">
Read `tests.filterRegression`. It is an object keyed by variant name, not a status array. Compare `ratio` against the `2.5` threshold yourself.

```json
{
  "tests": {
    "filterRegression": {
      "category=A3": { "p50": 24, "p95": 31, "baselineP95": 29, "ratio": 1.07 },
      "altitude range": {
        "p50": 26,
        "p95": 33,
        "baselineP95": 28,
        "ratio": 1.18
      }
    }
  }
}
```

Below `2.5` means the variant did not regress beyond the threshold. If a ratio is above `2.5`, the PR should explain why.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Verify the SQL matches your app code">
Use the `sql` field in each test entry to confirm the report executed the query path the PR claims to optimize. For example:

```json
{
  "tests": {
    "baseline": {
      "sql": "SELECT current_database, count() FROM ..."
    }
  }
}
```

If that SQL does not match the app code or benchmark target in the PR, stop and ask why.

</GuideStepper.Checkpoint>

</GuideStepper.Step>

<GuideStepper.Step
  id="review-migration-plan-and-changes"
  number={2}
  title="Review the migration plan and the code changes"
  summary="Read migrations/plan.yaml and the diff so you know exactly what will change in ClickHouse."
>
<GuideStepper.WhatYouNeed>
- The same PR
- Comfort reading `migrations/plan.yaml` and TypeScript data model diffs
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- A clear picture of schema operations (and app code changes) before they hit production
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Find the new versioned table in the diff">
In the diff, the optimized layout should appear as a new **`OlapTable` version**; leave the old version unchanged. For example:

```diff
 export const UserEventsV1 = new OlapTable("UserEvents", {
   version: "1.0",
   orderByFields: ["timestamp", "current_database"],
 });

+export const UserEventsV2 = new OlapTable("UserEvents", {
+  version: "2.0",
+  orderByFields: ["current_database", "timestamp"],
+});
```

Validate: **bumped `version`** on the new definition, previous version unchanged. Storage change with no new `version` on `OlapTable` needs an explanation.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Confirm the benchmark reads from the new model">
The benchmark should read the **new versioned table**, not the old one. Expect a small diff:

```diff
-import { UserEventsV1 } from "./models";
+import { UserEventsV2 } from "./models";

 const query = sql`
   SELECT count()
-  FROM ${UserEventsV1}
+  FROM ${UserEventsV2}
   WHERE timestamp >= ${start}
 `;
```

Schema, benchmark, and claim should line up. If the summary promises better skipping on `current_database` but the benchmark still hits the old table or the diff is unrelated noise, ask before merge.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Open migrations/plan.yaml and match the plan shape">
Open `migrations/plan.yaml`. Operations should match the versioned table change in code. Flag unexpected `DropTable` or `DropTableColumn`. Re-seeds need a note in the PR. See [Planned Migrations](/moosestack/migrate/planned-migrations).

Staged rollout shape:

```yaml
operations:
  - CreateTable:
      name: UserEvents_2_0
      columns: ...
      orderBy: [current_database, timestamp]
  - RawSql:
      sql: |
        INSERT INTO UserEvents_2_0
        SELECT *
        FROM UserEvents_1_0
```

Create table, then backfill. No destructive lifecycle in this PR. Code shows `version: "2.0"` but the plan has no new table plus copy? Ask why.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Validate the backfill and keep the old table">
`ORDER BY` or a rebuild needs a **backfill** in the plan, not only `CreateTable`. Usually a late **`RawSql`** `INSERT INTO <new> SELECT ... FROM <old>`.

Good shape:

```yaml {5-9}
operations:
  - CreateTable:
      name: UserEvents_2_0
      orderBy: [current_database, timestamp]
  - RawSql:
      sql: |
        INSERT INTO UserEvents_2_0
        SELECT *
        FROM UserEvents_1_0
```

New table with no backfill or `RawSql` copy? Request changes.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Look for unexpected DropTable operations">
**Do not drop the old table in the optimization PR.** Keep it as fallback until the new layout is validated.

Bad shape:

```yaml {5-6}
operations:
  - CreateTable:
      name: UserEvents_2_0
      orderBy: [current_database, timestamp]
  - DropTable:
      name: UserEvents_1_0
```

Early `DropTable` on the old table? Fix the plan before merge.

Removing `events_old` or other fallbacks belongs in a **follow-up PR**, not this one.

</GuideStepper.Checkpoint>
</GuideStepper.Step>

<GuideStepper.Step
  id="reproduce-benchmarks-baseline-vs-optimized-preview"
  number={3}
  title="Reproduce the benchmark tests yourself (optional)"
  summary="Re-run the same tests against the baseline and optimized preview databases to validate results on your machine."
>
<GuideStepper.WhatYouNeed>
- Credentials or env files for the two preview databases the agent created: the unchanged baseline fork and the optimized fork
- The test commands documented in the PR
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- Independent confirmation that the optimized preview beats, or at least matches, the baseline preview for the same test code
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Point tests at the baseline fork, then at the optimized fork">
The optimization workflow should leave you with a **Vitest** benchmark and helper scripts for rerunning it. Use that same benchmark and only change the target database. The generated `pnpm test:perf` script should already load `.env.remote`.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Find the database names for the two preview forks">
Between the baseline preview fork and the optimized preview fork, the ClickHouse **host**, **port**, **user**, **password**, and **SSL setting** are the same. They share one 514-hosted ClickHouse cluster. The only value that changes is the **database name**.

Use `514 agent deployment list` to find the database names for each branch.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Create a shared .env.remote file">
The `.env.remote` file should contain the shared connection settings **plus** your default database name. Use the baseline preview fork as the default so your first rerun is the unchanged comparison target:

```bash
MOOSE_CLICKHOUSE_CONFIG__HOST=<your-cluster>.clickhouse.boreal.cloud
MOOSE_CLICKHOUSE_CONFIG__HOST_PORT=8443
MOOSE_CLICKHOUSE_CONFIG__USER=default
MOOSE_CLICKHOUSE_CONFIG__PASSWORD=<your-password>
MOOSE_CLICKHOUSE_CONFIG__DB_NAME=<baseline-preview-db-name>
MOOSE_CLICKHOUSE_CONFIG__USE_SSL=true
```

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Swap only the database name between runs">
Then swap only the database name between runs:

```bash
# Test against baseline (uses DB_NAME from .env.remote)
pnpm test:perf

# Test against optimized preview fork
MOOSE_CLICKHOUSE_CONFIG__DB_NAME=<optimized-preview-db-name> pnpm test:perf
```

</GuideStepper.Checkpoint>
</GuideStepper.Step>

<GuideStepper.Step
  id="decide-whether-to-merge"
  number={4}
  title="Decide whether to merge"
  summary="Make the final shipping decision after review and optional reruns."
>
<GuideStepper.WhatYouNeed>
- Benchmark review complete
- Migration plan review complete
- Optional reruns complete, if you chose to do them
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- A clear merge or request-changes decision
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Merge when you are satisfied">
Merge when benchmarks, `migrations/plan.yaml`, and optional reruns match what you want to ship. Otherwise request changes.

</GuideStepper.Checkpoint>
</GuideStepper.Step>
</GuideStepper>

## Benchmark JSON quick reference

Use this when you need the full field map while reviewing the benchmark step above.

```json
{
  "timestamp": "2026-03-23T18:12:00.000Z",
  "target": {
    "host": "cluster.clickhouse.boreal.cloud",
    "database": "514-demos-planes-perf-base-59be4"
  },
  "tests": {
    "baseline": {
      "sql": "SELECT ...",
      "p50": 38,
      "p95": 44,
      "profiles": [{ "durationMs": 42 }]
    },
    "filterRegression": {
      "category=A3": { "p50": 24, "p95": 31, "baselineP95": 29, "ratio": 1.07 }
    },
    "explainCategoryFilter": {
      "sql": "SELECT ...",
      "explain": { "indexCondition": "..." }
    },
    "concurrentBurst": { "concurrency": 10, "errors": 0 },
    "clusterState": {},
    "connectionDiagnostics": {}
  }
}
```

`target` tells you which database ran the test. `tests.*` holds the evidence you compare.

| What to compare    | Where in the report                                  | What good looks like              |
| ------------------ | ---------------------------------------------------- | --------------------------------- |
| Index used?        | `tests.explainCategoryFilter.explain.indexCondition` | Not `"true"`                      |
| Granules skipped?  | `tests.explainCategoryFilter.explain.granuleSkipPct` | Higher in optimized than baseline |
| Server-side p95?   | `tests.baseline.p95`                                 | Lower in optimized than baseline  |
| Rows read?         | `tests.baseline.profiles[0].readRows`                | Lower in optimized                |
| Memory usage?      | `tests.baseline.profiles[0].memoryUsage`             | Lower in optimized                |
| Filter regression? | `tests.filterRegression.<variant>.ratio`             | Below `2.5`                       |

## Related docs

- [Hosting](/hosting)
- [`514 auth`](/hosting/cli/auth)
- [`514 project link`](/hosting/cli/projects/link)
- [Fiveonefour hosting to local setup](/hosting/workflow/fiveonefour-to-local-setup)
- [Setup failures](/hosting/troubleshooting/setup-failures)
- [Query Layer](/moosestack/reference/query-layer)
- [Semantic Layer](/moosestack/apis/semantic-layer)
- [Planned Migrations](/moosestack/migrate/planned-migrations)
