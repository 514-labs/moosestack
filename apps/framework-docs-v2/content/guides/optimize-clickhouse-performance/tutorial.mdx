---
title: Optimizing ClickHouse Performance with AI
description: Profile a slow query, compare a frozen baseline against optimization candidates, and review the winning branch before merge.
languages: ["typescript"]
---

import { Callout, GuideStepper } from "@/components/mdx";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";

# Optimizing ClickHouse Performance with AI

Use this guide when a ClickHouse query is slow and you want the `514` agent harness to run an end-to-end optimization workflow. The agent does not just make one guess. It profiles the production query path, proposes one or more candidate schema changes, freezes a `perf/baseline` branch, wires the same benchmark target across branches, benchmarks each candidate against that baseline, and opens a PR for the winning branch. Because the baseline and candidate branches share the same query contract and comparable preview data, you can review the result like a controlled experiment instead of a one-off claim.

Your role is to make sure the CLI and agent harness are set up in your environment, approve raw ClickHouse queries when prompted, and review the winning branch at the end. The artifacts that matter for that review are the scaffolded `query-benchmarks/` package, the baseline benchmark report, the winning candidate benchmark report, and a reviewable `migrations/plan.yaml` so you can confirm the schema change is safe to merge and will not accidentally ship an unintended database change.

<Callout type="info" title="What success looks like">
- A scaffolded `query-benchmarks/` package that exercises the real production query path
- A frozen baseline branch and one or more candidate branches compared with the same test
- A PR for the winning candidate with benchmark evidence and a reviewed `migrations/plan.yaml`
</Callout>

This guide assumes:

- Your MooseStack app is already deployed on Fiveonefour
- The agent can either reuse an existing query path in code or define a query model when profiling uncovers a production pattern without one

### Setup

**Setup is the control layer.** The workflow is going to run real `514` commands against a real Fiveonefour project, and the agent will inherit the same session and repo link. Get this wrong and the rest of the guide is noise.

You need: working `514 --version`, `514 auth whoami` for the right org, and `514 link` (or [`514 project link`](/hosting/cli/projects/link)) from the repo root to the correct `org/project`.

Order: install, sign in, link.

<GuideStepper id="optimize-ch-setup" persist>
<GuideStepper.Step
  id="install-514-cli"
  number={1}
  title="Install the 514 CLI"
  summary="Install the CLI from Hosting and verify it is on your PATH."
>
<GuideStepper.WhatYouNeed>
- A local clone of the MooseStack app repository
- [Hosting](/hosting) docs if install fails
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- `514` installed and `514 --version` working
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Install the 514 CLI">
From [Hosting](/hosting), install the `514` CLI:

```bash
bash -i <(curl -fsSL https://fiveonefour.com/install.sh) 514
```
</GuideStepper.Checkpoint>
<GuideStepper.Checkpoint title="Verify installation">

```bash
514 --version
```

</GuideStepper.Checkpoint>
</GuideStepper.Step>

<GuideStepper.Step
  id="sign-in-514"
  number={2}
  title="Sign in to the 514 CLI"
  summary="Establish a session so project and deployment commands work."
>
<GuideStepper.WhatYouNeed>
- Install step complete
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- A confirmed account via `514 auth whoami`
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Authenticate">
```bash
514 auth login
514 auth whoami
```

Browser opens for login, then `514 auth whoami` shows your account. No browser? Sign in at `https://www.fiveonefour.boreal.cloud/login` and retry.
Related docs:

- [`514 auth login`](/hosting/cli/auth/login)
- [`514 auth`](/hosting/cli/auth)

</GuideStepper.Checkpoint>
</GuideStepper.Step>

<GuideStepper.Step
  id="link-fiveonefour-project"
  number={3}
  title="Link your local repo to your Fiveonefour project"
  summary="Align local workspace with your hosted org and project."
>
<GuideStepper.WhatYouNeed>
- Access to the Fiveonefour project that hosts the deployment you want to optimize
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- Local configuration that points at the right `org/project` for CLI and agents
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Link your local project to your Fiveonefour project">
Make sure you are in the root of your MooseStack project directory and run:

```bash
514 project link <org>/<project>
```

Related docs:

- [`514 project link`](/hosting/cli/projects/link)
- [Fiveonefour hosting to local setup](/hosting/workflow/fiveonefour-to-local-setup)
- [Setup failures](/hosting/troubleshooting/setup-failures)

</GuideStepper.Checkpoint>
</GuideStepper.Step>
</GuideStepper>

### Optimization workflow

**This section gets the 514 harness set up with your coding agent and runs the optimization workflow.** After `514 agent init`, the workflow uses the `514` CLI to inspect ClickHouse metrics and logs, profile the slow path, scaffold a benchmark package with `moose add benchmark`, propose candidate fixes based on what it actually sees, freeze a control branch, and benchmark candidate branches against it. Part of that run is wiring a benchmark target that stays the same across baseline and candidate branches. The point is not to prove that one preview database is faster. The point is to compare the same query path across a control and one or more candidates, then ship the winner.

Watch three things as it runs:

- **The harness is active:** `514 agent init` succeeds and the skills and MCP wiring match your setup.
- **The profiling target is clear:** name the slow query or endpoint and confirm the perf skill, not a generic refactor.
- **The experiment path is visible:** proposed candidates, the frozen baseline, candidate branches, raw SQL, and any re-seed caused by `ORDER BY` or table layout changes should all be explicit.

<GuideStepper id="optimize-ch-workflow" persist>
<GuideStepper.Step
  id="agent-init-harness"
  number={1}
  title="Run 514 agent init"
  summary="Pull in 514 skills and scaffold the agent harness for your repo."
>
<GuideStepper.WhatYouNeed>
- Setup complete: CLI installed, signed in, project linked
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- Agent skill packs (including ClickHouse and performance-related workflows where applicable)
- MCP and harness files your coding agent uses to run `514`-backed workflows
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Initialize the harness">

```bash
514 agent init
```

This pulls skills from the 514 registry and sets up the agent harness for your project.
</GuideStepper.Checkpoint>
</GuideStepper.Step>

<GuideStepper.Step
  id="ask-copilot-optimize"
  number={2}
  title="Ask your copilot to optimize a query"
  summary="Prompt your coding agent and confirm the performance optimization skill."
>
<GuideStepper.WhatYouNeed>
- A deployed MooseStack app on Fiveonefour (the slow query path in code)
- Your coding agent (Cursor, Claude Code, Copilot in your editor, and so on) opened on this repo
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- The agent selects the performance optimization skill (often named `514--perf-optimize` or `perf-optimize` depending on client)
- Explicit confirmation before it runs a long optimization workflow
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Prompt the agent">
Ask for a ClickHouse performance optimization on your deployed app, and point it at the slow query or endpoint. Example:

```text
Optimize ClickHouse performance for this MooseStack app on Fiveonefour.
Profile the slow query path, propose candidate schema changes, create a frozen baseline branch,
compare candidate branches against the same benchmark, and prepare a PR for the winner with benchmark evidence.
```

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Confirm the performance optimization skill">
When the agent proposes the perf optimization skill, confirm. It should drive profiling, schema work, benchmarks, and the PR. If the bundle name differs, accept only if it is the same harness you got from `514 agent init`.

</GuideStepper.Checkpoint>
</GuideStepper.Step>

<GuideStepper.Step
  id="optimization-workflow-runs"
  number={3}
  title="Let the optimization workflow run"
  summary="The agent profiles the query, proposes candidates, freezes a baseline, benchmarks candidates against it, compares results, and opens a PR for the winner."
>
<GuideStepper.WhatYouNeed>
- Patience for branch pushes and preview deployments
- Willingness to approve raw `514 clickhouse query` when the skill prompts you
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- A frozen `perf/baseline` branch with the benchmark target wired up
- One or more candidate branches with data model and benchmark target changes
- Benchmark runs against baseline and candidate deployments, and a PR for the winner
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Expect seven internal stages">
The workflow follows `SETUP → PROFILE → PROPOSE → BASELINE → EXPERIMENT → COMPARE → SHIP`.

- **SETUP:** CLI session, project, deployment, baseline DDL.
- **PROFILE:** Slow queries, `EXPLAIN`, storage diagnostics, and the benchmark query set.
- **PROPOSE:** Candidate schema changes, risks, and re-seed categories.
- **BASELINE:** Benchmark scaffold, target interface, frozen `perf/baseline`, and a proven control run.
- **EXPERIMENT:** Candidate branches, preview deploys, re-seeds if needed, and benchmark runs.
- **COMPARE:** Baseline versus candidates, regressions, and winner selection.
- **SHIP:** PR for the winning candidate with evidence and summary.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Know what the benchmark scaffold creates">
The workflow should run `moose add benchmark` and reuse the generated package instead of hand-writing benchmark infrastructure. In the prototype, that scaffold looks like this:

```text
query-benchmarks/
  benchmark.config.ts
  benchmark/core.ts
  tests/query-benchmarks.test.ts
  package.json
```

Treat that package as part of the review surface. `benchmark.config.ts` is where the benchmark target, scenarios, thresholds, and sampling live. `tests/query-benchmarks.test.ts` is the generated runner that executes the contract and writes the reports.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Understand the benchmark target contract">
The benchmark target interface is the code-level representation of the production query pattern the agent discovered. It can be an existing `defineQueryModel`, an existing query builder, or a new query model the agent creates if profiling finds a real query pattern without a codified entrypoint.

What should stay the same across baseline and candidate branches:

- the same base query or API entrypoint
- the same scenarios and filters
- the same thresholds and sampling
- the same query shape apart from the intended optimization

What is allowed to change per candidate branch:

- the minimal table or model reference needed to point at the optimized layout

Example:

```ts
const baseQuery = () => buildQuery(benchmarkModel);

export const benchmark = defineBenchmark({
  baseQuery,
  scenarios: [
    {
      name: "category=A3",
      query: () => baseQuery().filter("category", "eq", "A3"),
    },
  ],
  thresholds: {
    baselineP95Ms: 500,
    scenarioRegressionRatio: 2.5,
  },
});
```

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Approve gated ClickHouse commands">
The `514 agent ...` workflow is guardrailed, so its built-in steps run without extra approval. Raw `514 clickhouse query` (DDL, `EXPLAIN`, diagnostics, re-seed) should prompt you. Approve when the SQL matches intent.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Watch for candidate re-seeds">
If `ORDER BY` or similar forces a rebuild, each candidate branch may need a re-seed before benchmarks are trustworthy. The PR should say which candidate needed it and how the data was copied forward.

</GuideStepper.Checkpoint>

<Callout type="info" title="What good evidence looks like">
Example: index use, fewer granules read (for example selective condition vs full scan, high `granuleSkipPct`), lower server-side latency. Your PR should show that kind of structural move in `EXPLAIN`, not only a headline number.
</Callout>
</GuideStepper.Step>
</GuideStepper>

### PR review

**This is the part that matters.** The agent can open a PR. You still decide what ships. Your job here is to check that the change is safe and that the evidence matches reality.

- **Performance:** Do benchmarks and `EXPLAIN` back the claim, not just the PR text?
- **Destructive ops:** Does `ORDER BY` or ordering change imply a rebuild you accept? Read `migrations/plan.yaml` with that in mind.

**Read `migrations/plan.yaml` end to end.** If `ORDER BY` or a rebuild is involved, expect a **backfill** or migration path, not only a new table. Keep the **old table** until the new one is safe. Populate and verify before drop or rename.

Order: benchmarks, then plan and diff, then optional reruns.

<GuideStepper id="optimize-ch-pr" persist>
<GuideStepper.Step
  id="review-benchmark-results"
  number={1}
  title="Review the benchmark results"
  summary="Read the benchmark package and benchmark JSON to verify the evidence in the PR."
>
<GuideStepper.WhatYouNeed>
- The PR the agent opened
- The committed `query-benchmarks/` package and any `reports/benchmark-details-*.json` attached or generated in CI for both the frozen baseline branch and the winning candidate branch
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- Confidence that the winning candidate branch actually beats the baseline for the same query path, without hiding a scenario-level regression
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Identify the winner report and the baseline report">
Pair two artifacts: **winner** (the candidate branch the agent selected) and **baseline** (the frozen control branch with no schema change). The report usually does not include a branch field. In practice, the **database name contains the branch slug**, so that is the field to inspect first.

<Tabs defaultValue="winner-report">
  <TabsList>
    <TabsTrigger value="winner-report">Winner</TabsTrigger>
    <TabsTrigger value="baseline-report">Baseline</TabsTrigger>
  </TabsList>
  <TabsContent value="winner-report">

```json
{
  "target": {
    "host": "cluster.clickhouse.boreal.cloud",
    "database": "514-demos-planes-perf-opti-1c360"
  }
}
```

  </TabsContent>
  <TabsContent value="baseline-report">

```json
{
  "target": {
    "host": "cluster.clickhouse.boreal.cloud",
    "database": "514-demos-planes-perf-base-59be4"
  }
}
```

  </TabsContent>
</Tabs>

Both reports must use the **same benchmark package** and query path contract, different targets only. No clear baseline means no real before/after.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Read the scaffolded benchmark package">
Open the committed benchmark package, usually `query-benchmarks/`. Start with `query-benchmarks/benchmark.config.ts`, then read `query-benchmarks/tests/query-benchmarks.test.ts`.

What to confirm before you trust any report:

- `benchmark.config.ts` points at the real query target the PR claims to optimize
- `baseQuery`, scenarios, thresholds, and sampling are the same between baseline and winner
- the generated test runner is still exercising that same contract
- only the minimal target change was introduced for the winner, usually a table or model reference

If the benchmark package changed in broader ways than that, the comparison is no longer clean.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Check how the winner was chosen">
The PR summary should compare the frozen baseline against each candidate branch. Read that comparison before you inspect any single JSON field.

The winner should be selected because it does the best job on the same contract, not because one headline number looked better in isolation. Good winner-selection language usually includes:

- lower overall `tests.baseline.p95` than baseline
- no scenario regression ratio above the configured threshold
- at least one selective scenario showing better pruning or fewer granules read
- caveats called out when a scenario stays flat or the base query still full-scans

If another candidate looks stronger on those same measures, ask why it was not selected.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Start with scenario regressions, not the base query">
Open `tests.scenarioRegression` first. This is where the selective scenarios usually reveal whether the optimization helped the real filtered paths you care about.

What to look for:

- which scenarios improved materially
- whether any scenario crossed the configured regression threshold
- whether one scenario explains most of the win while the others stay flat
- whether the PR caveats match what the scenario data actually shows

<Tabs defaultValue="winner-explain">
  <TabsList>
    <TabsTrigger value="winner-explain">Winner</TabsTrigger>
    <TabsTrigger value="baseline-explain">Baseline</TabsTrigger>
  </TabsList>
  <TabsContent value="winner-explain">

```json
{
  "tests": {
    "scenarioRegression": {
      "category=A3": {
        "p50": 18,
        "p95": 52,
        "baselineP95": 396,
        "ratio": 0.13
      },
      "altitude>10000": {
        "p50": 126,
        "p95": 129,
        "baselineP95": 396,
        "ratio": 0.33
      }
    }
  }
}
```

  </TabsContent>
  <TabsContent value="baseline-explain">

```json
{
  "tests": {
    "scenarioRegression": {
      "category=A3": {
        "p50": 172,
        "p95": 189,
        "baselineP95": 326,
        "ratio": 0.58
      },
      "altitude>10000": {
        "p50": 188,
        "p95": 192,
        "baselineP95": 326,
        "ratio": 0.59
      }
    }
  }
}
```

  </TabsContent>
</Tabs>

The important comparison is not whether every scenario improved equally. It is whether any scenario regressed past threshold and whether the winning candidate improved the scenarios that matter.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Read EXPLAIN at the scenario level">
Then look at `tests.explain`. Do not assume the unfiltered base query must stop returning `indexCondition: "true"`. In the current planes-style benchmark, the more important question is whether at least one **selective scenario** starts pruning where the baseline did not.

Good shape:

- baseline can still full-scan
- one or more selective scenarios show a narrower `indexCondition`
- selective scenarios read fewer granules in the winner than in the baseline

Example:

```json
{
  "tests": {
    "explain": {
      "baseline": {
        "explain": {
          "indexCondition": "true",
          "granuleSkipPct": 0
        }
      },
      "category=A3": {
        "explain": {
          "indexCondition": "category in ['A3']",
          "granuleSkipPct": 75
        }
      }
    }
  }
}
```

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Compare overall latency and resource usage">
After the scenario checks, look at `tests.baseline.p50`, `tests.baseline.p95`, and the per-run objects in `tests.baseline.profiles`. The `profiles` array is where the useful resource metrics live.

<Tabs defaultValue="winner-latency">
  <TabsList>
    <TabsTrigger value="winner-latency">Winner</TabsTrigger>
    <TabsTrigger value="baseline-latency">Baseline</TabsTrigger>
  </TabsList>
  <TabsContent value="winner-latency">

```json
{
  "tests": {
    "baseline": {
      "p50": 38,
      "p95": 44,
      "profiles": [
        {
          "durationMs": 42,
          "readRows": 180000,
          "readBytes": 12000000,
          "memoryUsage": 6400000,
          "diskReadUs": 1200
        }
      ]
    }
  }
}
```

  </TabsContent>
  <TabsContent value="baseline-latency">

```json
{
  "tests": {
    "baseline": {
      "p50": 119,
      "p95": 126,
      "profiles": [
        {
          "durationMs": 121,
          "readRows": 920000,
          "readBytes": 68000000,
          "memoryUsage": 15400000,
          "diskReadUs": 8800
        }
      ]
    }
  }
}
```

  </TabsContent>
</Tabs>

Good sign: lower `p95`, fewer rows read, and lower memory usage in the winner report. This is the broad query-level effect. The scenario regressions and scenario-level `EXPLAIN` tell you where that improvement came from.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Verify the SQL matches your app code">
Use the `sql` field in each test entry to confirm the report executed the query path the PR claims to optimize. For example:

```json
{
  "tests": {
    "baseline": {
      "sql": "SELECT current_database, count() FROM ..."
    }
  }
}
```

If that SQL does not match the app code or benchmark target in the PR, stop and ask why.

</GuideStepper.Checkpoint>

</GuideStepper.Step>

<GuideStepper.Step
  id="review-migration-plan-and-changes"
  number={2}
  title="Review the migration plan and rollout intent"
  summary="Read migrations/plan.yaml as a sequence so you know what changes in ClickHouse, how data moves, and whether rollout planning should reshape the plan before production."
>
<GuideStepper.WhatYouNeed>
- The same PR
- Comfort reading `migrations/plan.yaml` and TypeScript data model diffs
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- A clear picture of schema operations, data movement, and rollout questions before anything hits production
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Match the schema diff, benchmark target, and plan">
Start in the code diff. Find the schema change the PR says made the query faster, then make sure the benchmark and the migration plan both point at that same change.

One common shape is a new `OlapTable` version:

```diff
 export const UserEventsV1 = new OlapTable("UserEvents", {
   version: "1.0",
   orderByFields: ["timestamp", "current_database"],
 });

+export const UserEventsV2 = new OlapTable("UserEvents", {
+  version: "2.0",
+  orderByFields: ["current_database", "timestamp"],
+});
```

If the PR claims better skipping on `current_database`, the benchmark should target the new layout and `migrations/plan.yaml` should explain how the database gets there. If the diff, benchmark target, and plan all tell different stories, stop and ask why.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Read plan.yaml as a sequence, not just a list">
Open `migrations/plan.yaml` and read it top to bottom. You are not only checking whether the right operations appear. You are checking whether the **sequence makes sense**.

For each operation, ask:

- what problem is this step solving
- what data exists before this step
- what data exists after this step
- does the next step depend on this one having happened first

Good review language sounds like: rename the old table to preserve the current rows, create the new layout, backfill into the new layout, then clean up the old copy when the move is complete.

See [Planned Migrations](/moosestack/migrate/planned-migrations) for the plan format.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Treat rename-create-backfill-drop as a valid reviewed shape">
Do not use "no drop ever" as the rule. A reviewed sequence can rename the old table, create a new one, backfill it, and then drop the renamed old table after the copy.

One valid shape looks like this:

```yaml
operations:
  - RawSql:
      sql: |
        RENAME TABLE UserEvents TO UserEvents_old
  - CreateTable:
      name: UserEvents
      orderBy: [current_database, timestamp]
  - RawSql:
      sql: |
        INSERT INTO UserEvents
        SELECT *
        FROM UserEvents_old
  - RawSql:
      sql: |
        DROP TABLE IF EXISTS UserEvents_old
```

That can be correct when the rename preserves the original rows long enough for the backfill and the drop only happens after the move is done.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Check the backfill details, including casts">
Rebuild-style optimizations need a data-preserving copy step, not just `CreateTable`. In `RawSql` backfills, check whether the selected columns line up with the new table definition.

If the new layout changes data types, codecs, or `LowCardinality` usage, explicit casts in the backfill can be a good sign:

```yaml
operations:
  - RawSql:
      sql: |
        INSERT INTO UserEvents
        SELECT
          timestamp,
          CAST(user_id, 'LowCardinality(String)'),
          current_database
        FROM UserEvents_old
```

You are looking for a believable story: rows move from the preserved old shape into the new shape without silent type mismatch or missing columns. A new table with no backfill, or a backfill that ignores a type change the schema diff introduced, needs an explanation.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Treat destructive operations as review points, not auto-rejects">
`DropTable`, `DropTableColumn`, and `ModifyTableColumn` are not automatically wrong. They are operations that need intent, timing, and consequences explained. The [going-to-production migration review](/guides/applications/going-to-production) is the stricter reference for shaping these operations before a production deploy.

Good signs:

- a `DropTable` only removes a renamed old table after a successful backfill
- a risky column operation is clearly tied to an intentional schema change
- the PR calls out any re-seed, backfill, or recovery implications

Stop and request changes when:

- a destructive step removes an active table or column with no preservation step
- the plan shape does not match the code diff
- preview behavior suggests the plan did something other than what the PR describes

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Hand off to rollout planning when the plan needs reshaping">
This optimization review is about whether the winning candidate is believable and intentional. It is **not** the final word on the safest production rollout shape.

If the plan needs phased rollout logic, manual verification gates, a delayed cleanup step, or a more cautious recovery path, stop here and hand it off to rollout planning before merge. The optimization workflow found a promising winner. Rollout planning decides whether that exact migration sequence is the one you should ship to production. Use the [going-to-production guide](/guides/applications/going-to-production) when you need the stricter production rollout checklist.

</GuideStepper.Checkpoint>
</GuideStepper.Step>

<GuideStepper.Step
  id="reproduce-benchmarks-baseline-vs-winner-preview"
  number={3}
  title="Reproduce the benchmark tests yourself (optional)"
  summary="Re-run the same tests against the baseline and winning candidate preview databases to validate results on your machine."
>
<GuideStepper.WhatYouNeed>
- Credentials or env files for the two preview databases the agent created: the frozen baseline fork and the winning candidate fork
- The test commands documented in the PR
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- Independent confirmation that the winning candidate preview beats, or at least matches, the baseline preview for the same test code
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Point tests at the baseline fork, then at the winner">
The optimization workflow should leave you with a **Vitest** benchmark and helper scripts for rerunning it. Use that same benchmark and only change the target database. The generated `pnpm test:perf` script should already load `.env.remote`.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Find the database names for the baseline and winning candidate forks">
Between the baseline preview fork and the winning candidate preview fork, the ClickHouse **host**, **port**, **user**, **password**, and **SSL setting** are the same. They share one 514-hosted ClickHouse cluster. The only value that changes is the **database name**.

Use `514 agent deployment list` to list the agent-visible deployments for the project and find the database names for each branch. See [`514 agent`](/hosting/cli/agent) for command context.

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Create a shared .env.remote file">
The `.env.remote` file should contain the shared connection settings **plus** your default database name. Use the baseline preview fork as the default so your first rerun is the unchanged comparison target:

```bash
MOOSE_CLICKHOUSE_CONFIG__HOST=<your-cluster>.clickhouse.boreal.cloud
MOOSE_CLICKHOUSE_CONFIG__HOST_PORT=8443
MOOSE_CLICKHOUSE_CONFIG__USER=default
MOOSE_CLICKHOUSE_CONFIG__PASSWORD=<your-password>
MOOSE_CLICKHOUSE_CONFIG__DB_NAME=<baseline-preview-db-name>
MOOSE_CLICKHOUSE_CONFIG__USE_SSL=true
```

</GuideStepper.Checkpoint>

<GuideStepper.Checkpoint title="Swap only the database name between runs">
Then swap only the database name between runs:

```bash
# Test against baseline (uses DB_NAME from .env.remote)
pnpm test:perf

# Test against winning candidate preview fork
MOOSE_CLICKHOUSE_CONFIG__DB_NAME=<winning-candidate-preview-db-name> pnpm test:perf
```

</GuideStepper.Checkpoint>
</GuideStepper.Step>

<GuideStepper.Step
  id="decide-whether-to-merge"
  number={4}
  title="Decide whether to merge"
  summary="Make the final shipping decision after review and optional reruns."
>
<GuideStepper.WhatYouNeed>
- Benchmark review complete
- Migration plan review complete
- Optional reruns complete, if you chose to do them
</GuideStepper.WhatYouNeed>
<GuideStepper.WhatYouGet>
- A clear merge or request-changes decision
</GuideStepper.WhatYouGet>

<GuideStepper.Checkpoint title="Merge when you are satisfied">
Merge when benchmarks, `migrations/plan.yaml`, and optional reruns match what you want to ship. Otherwise request changes.

</GuideStepper.Checkpoint>
</GuideStepper.Step>
</GuideStepper>

## Benchmark JSON quick reference

Use this when you need the full field map while reviewing the benchmark step above.

```json
{
  "timestamp": "2026-03-23T18:12:00.000Z",
  "target": {
    "host": "cluster.clickhouse.boreal.cloud",
    "database": "514-demos-planes-perf-base-59be4"
  },
  "tests": {
    "baseline": {
      "sql": "SELECT ...",
      "p50": 38,
      "p95": 44,
      "profiles": [{ "durationMs": 42 }]
    },
    "scenarioRegression": {
      "category=A3": { "p50": 24, "p95": 31, "baselineP95": 29, "ratio": 1.07 }
    },
    "explain": {
      "baseline": {
        "sql": "SELECT ...",
        "explain": { "indexCondition": "true", "granuleSkipPct": 0 }
      },
      "category=A3": {
        "sql": "SELECT ... WHERE category = 'A3'",
        "explain": { "indexCondition": "category in ['A3']", "granuleSkipPct": 75 }
      }
    },
    "concurrentBurst": { "concurrency": 10, "errors": 0 },
    "clusterState": {},
    "connectionDiagnostics": {}
  }
}
```

`target` tells you which database ran the test. `tests.*` holds the evidence you compare.

| What to compare               | Where in the report                             | What good looks like                                |
| ----------------------------- | ----------------------------------------------- | --------------------------------------------------- |
| Overall p95?                  | `tests.baseline.p95`                            | Lower in winner than baseline                       |
| Scenario regressions?         | `tests.scenarioRegression.<variant>.ratio`      | No scenario above the configured threshold          |
| Selective scenario pruning?   | `tests.explain.<scenario>.explain.indexCondition` | At least one selective scenario is not `"true"`   |
| Granules skipped per scenario | `tests.explain.<scenario>.explain.granuleSkipPct` | Higher in winner for the selective scenario       |
| Rows read?                    | `tests.baseline.profiles[0].readRows`           | Lower in winner when the contract is unchanged      |
| Memory usage?                 | `tests.baseline.profiles[0].memoryUsage`        | Lower in winner when the contract is unchanged      |

## Related docs

- [Hosting](/hosting)
- [`514 auth`](/hosting/cli/auth)
- [`514 project link`](/hosting/cli/projects/link)
- [Fiveonefour hosting to local setup](/hosting/workflow/fiveonefour-to-local-setup)
- [Setup failures](/hosting/troubleshooting/setup-failures)
- [Query Layer](/moosestack/reference/query-layer)
- [Semantic Layer](/moosestack/apis/semantic-layer)
- [Planned Migrations](/moosestack/migrate/planned-migrations)
