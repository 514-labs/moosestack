---
title: Overview
description: When to use the ClickHouse optimization workflow and what to expect from the review artifacts
languages: ["typescript"]
---

import { BulletPointsCard } from "@/components/mdx";

## Overview

This guide is for a deployed MooseStack application with a production ClickHouse query pattern that can be faster. The goal is not to teach ClickHouse from scratch. The goal is to help you use an AI workflow that can profile the slow path, scaffold a benchmark package around the real query contract, propose candidate schema changes, validate them on preview, and hand you back a PR for the winning candidate with evidence you can verify.

That workflow ends at a benchmarked winner and a reviewable migration plan. It does **not** decide the safest production rollout shape for every change. If the winning candidate needs a phased release, delayed cleanup, or stricter recovery plan, rollout planning takes over after the optimization review.

## Prerequisites

The workflow relies on the `514` CLI, a linked local repository, and the agent harness from `514 agent init`. In the [tutorial](/guides/optimize-clickhouse-performance/tutorial), complete **Setup** (install, sign in, link), then **Optimization workflow** (`514 agent init`, copilot prompt, run the skill), then **PR review** (benchmark results, migration plan and code diff, optional reruns on preview vs `main`). Hosting reference: [Hosting](/hosting), [`514 project link`](/hosting/cli/projects/link), [Fiveonefour hosting to local setup](/hosting/workflow/fiveonefour-to-local-setup), and [Setup failures](/hosting/troubleshooting/setup-failures) if something blocks you.

<BulletPointsCard
  title="This guide is a good fit when"
  bulletStyle="check"
  bullets={[
    {
      title: "You have production apps powered by ClickHouse that can be faster",
      description:
        "The workflow profiles a real production query path and benchmarks changes against it, so it works best when there is already a live workload to improve.",
    },
    {
      title: "You want your AI agent to help you get better at ClickHouse",
      description:
        "ClickHouse data modeling is tricky. The workflow lets your agent bridge that gap by profiling the real query pattern, proposing changes, and turning them into artifacts you can inspect.",
    },
    {
      title: "You want to review the agent's work before anything touches production",
      description:
        "The output is a pull request with benchmark test results and a migration plan that you review and approve.",
    },
  ]}
/>

## What the workflow is actually optimizing

ClickHouse performance usually comes down to whether your table ordering matches the filter and grouping pattern your application actually uses.

In the prototype case, the slow query path was not fixed by generic SQL cleanup. It got faster because the schema started matching the query shape, which changed the `EXPLAIN indexes = 1` output from a full scan:

```text
Condition: true
Granules: 5921/5921
```

to a useful index condition with meaningful skipping:

```text
Condition: category in ['A3']
Granules: 6/24
Granule skip: 75%
```

That is the main idea behind this guide. The workflow is trying to improve structural signals first, then confirm that latency follows.

## What the workflow gives back

You are not expected to trust the agent on vibes. The workflow should give you:

- a scaffolded `query-benchmarks/` package created by `moose add benchmark`, instead of a one-off benchmark file
- a `benchmark.config.ts` contract that keeps the same query target, scenarios, thresholds, and sampling across baseline and candidate branches
- `EXPLAIN` output showing whether the query started using the primary key or another skipping mechanism
- benchmark timing collected from server-side query data
- `migrations/plan.yaml` so you can review the schema operations and decide whether the winner needs additional rollout shaping before production

## Where optimization ends and rollout planning begins

The optimization review has two gates:

1. The benchmark evidence
2. The migration plan

The benchmark tells you whether the optimized query path is structurally better and measurably faster. The migration plan tells you whether the branch is changing the database in the way you intended.

If both look good, you have a believable winner. That still does not mean every candidate should ship to production unchanged. When the migration plan needs phased rollout logic, delayed cleanup, manual verification gates, or a stricter recovery path, that is the handoff point to rollout planning.

If you want the step-by-step path for running the workflow and reviewing those artifacts, continue to the tutorial.
