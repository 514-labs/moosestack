# Allow hoisting React types for this app to ensure TypeScript resolves React 19 types
# This overrides the root .npmrc which prevents hoisting to support multiple React versions
# Since this app only uses React 19, we can safely hoist React types here
public-hoist-pattern[]=@types/react
public-hoist-pattern[]=@types/react-dom

