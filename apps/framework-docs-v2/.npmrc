# Ensure React types are resolved from local package
# This prevents TypeScript from finding React 18 types in nested node_modules
public-hoist-pattern[]=@types/react
public-hoist-pattern[]=@types/react-dom

