# Python Cluster Test Template

This is a test-only template for E2E testing of ClickHouse cluster support in MooseStack.

## Features

- Three OLAP tables demonstrating different cluster configurations
- `table_a`: Uses `cluster_a`
- `table_b`: Uses `cluster_b`
- `table_c`: No cluster (for mixed environment testing)
