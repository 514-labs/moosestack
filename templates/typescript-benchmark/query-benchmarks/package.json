{
  "name": "query-benchmarks",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "env:preview": "514 env list --platform --dotenv > .env.preview",
    "bench": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  }
}
