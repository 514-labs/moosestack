language = "typescript" # Must be typescript or python
description = "Build a ClickHouse-backed analytical API with Fastify and TypeScript."
post_install_print = """
Deploy on Boreal

Boreal is the fastest way to deploy your MooseStack app, built by the MooseStack team.

https://boreal.cloud

---------------------------------------------------------

Run this next:

📂 Go to your project directory:
    $ cd {project_dir}

📦 Install dependencies:
    $ pnpm install

🛠️ Start the dev server:
    $ moose dev
"""
default_sloan_telemetry = "standard"

[boreal]
enabled = true
display_name = "Fastify API Starter"
short_description = "Expose ClickHouse analytics to apps through a high-performance Fastify + TypeScript API."
featured = false
