language = "typescript" # Must be typescript or python
description = "TypeScript project using Fastify for high-performance analytical APIs with WebApp integration."
post_install_print = """
Moose + Fastify Template

This template demonstrates using Fastify with MooseStack's WebApp class
to build high-performance analytical APIs.

---------------------------------------------------------

📂 Go to your project directory:
    $ cd {project_dir}

📦 Install Dependencies:
    $ npm install

🛠️ Start dev server:
    $ npx moose dev

🧪 Test the Fastify endpoints:
    $ curl http://localhost:4000/fastify/health
    $ curl http://localhost:4000/fastify/query?limit=5

---------------------------------------------------------

Deploy on Boreal

The easiest way to deploy your MooseStack Applications is to use Boreal
from the creators of MooseStack.

https://boreal.cloud
"""
default_sloan_telemetry = "standard"
