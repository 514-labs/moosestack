language = "typescript" # Must be typescript or python
description = "TypeScript project using Express for serving analytical APIs, with a frontend with chat and MCP configured"
post_install_print = """
Deploy on Boreal

The easiest way to deploy your MooseStack Applications is to use Boreal
from the creators of MooseStack.

https://boreal.cloud

---------------------------------------------------------

📂 Go to your project directory:
    $ cd {project_dir}

📦 Install MooseStack Dependencies:
    $ cd moosestack-service
    $ pnpm install
    $ moose dev # start moose dev server
    
⚠️ In a seperate terminal:
    $ cd {project_dir}

📦 Install Web App Dependencies:
    $ cd web-app-with-ai-chat
    $ pnpm install
    $ pnpm run dev # start web dev server
"""
default_sloan_telemetry = "standard"
