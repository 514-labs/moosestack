{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://moosejs.com/schemas/e2e-v2/capabilities.schema.json",
  "title": "Moose Template Capabilities",
  "description": "Schema for moose.capabilities.json files in templates",
  "type": "object",
  "required": ["capabilities"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference"
    },
    "capabilities": {
      "type": "array",
      "description": "List of capability identifiers supported by this template",
      "items": {
        "type": "string",
        "pattern": "^(lang|infra|feature|model):[a-zA-Z0-9_*-]+$"
      },
      "minItems": 1,
      "uniqueItems": true
    },
    "testPort": {
      "type": "integer",
      "description": "Port to use when running tests against this template",
      "minimum": 1024,
      "maximum": 65535
    },
    "skipScenarios": {
      "type": "array",
      "description": "List of scenario names to skip for this template",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    }
  },
  "additionalProperties": false
}
