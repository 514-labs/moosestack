name: Test VS Code Extension

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: Placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "VS Code extension test workflow skeleton. Implementation lands in the stacked PR."
